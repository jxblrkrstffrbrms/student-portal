import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pisay.student.portal',
  appName: 'pisay-student-portal',
  webDir: 'www'
};

export default config;
